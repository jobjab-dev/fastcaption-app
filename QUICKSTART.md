# 🚀 WhisperPro — Quick Start Testing Guide

## ขั้นตอนที่ 1: สร้าง AUTH_SECRET (30 วินาที)

```bash
cd d:\tiktok\webapp
npx auth secret
```

จะ append `AUTH_SECRET=...` ลงใน `.env.local` ให้อัตโนมัติ

---

## ขั้นตอนที่ 2: สร้าง Google OAuth (5 นาที)

1. ไปที่ **[console.cloud.google.com](https://console.cloud.google.com)**
2. สร้าง Project ใหม่ (หรือใช้อันเดิม)
3. ไปที่ **APIs & Services → Credentials → Create Credentials → OAuth Client ID**
4. เลือก **Web Application**
5. ตั้งค่า:
   - **Authorized JavaScript origins**: `http://localhost:3000`
   - **Authorized redirect URIs**: `http://localhost:3000/api/auth/callback/google`
6. Copy **Client ID** และ **Client Secret**

ใส่ลงไฟล์ `d:\tiktok\webapp\.env.local`:
```
GOOGLE_CLIENT_ID=xxxxxx.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-xxxxxxx
```

> [!TIP]
> ข้าม GitHub OAuth ได้ก่อน — ใช้แค่ Google ก็ test ได้

---

## ขั้นตอนที่ 3: สร้าง Stripe Test Keys (5 นาที)

1. ไปที่ **[dashboard.stripe.com](https://dashboard.stripe.com)**
2. สร้าง account (ฟรี, ไม่ต้อง verify ตอน test)
3. ไปที่ **Developers → API keys**
4. Copy **test mode** keys (ขึ้นต้นด้วย `sk_test_` และ `pk_test_`)

ใส่ลงไฟล์ `.env.local`:
```
STRIPE_SECRET_KEY=sk_test_xxxxxxx
STRIPE_PUBLISHABLE_KEY=pk_test_xxxxxxx
```

> [!NOTE]  
> Stripe Webhook Secret ยังไม่ต้องตั้ง — ใช้ตอน deploy จริงเท่านั้น

---

## ขั้นตอนที่ 4: รัน! (1 นาที)

```bash
cd d:\tiktok\webapp
npm run dev
```

เปิด **http://localhost:3000** ในเบราว์เซอร์

---

## ทดสอบ Flow

### 🔐 ทดสอบ Login
1. กดปุ่ม **"เข้าสู่ระบบ"**
2. เลือก **Google**
3. Login สำเร็จ → จะได้ **5,000 credits ฟรี** อัตโนมัติ
4. ถูก redirect ไป Dashboard

### 💳 ทดสอบ Stripe Payment
1. ไปหน้า **Pricing** → กด **"ซื้อเลย"**
2. Stripe Checkout จะเปิดขึ้น
3. ใช้ **เลขบัตรทดสอบ**: `4242 4242 4242 4242`
   - วันหมดอายุ: อะไรก็ได้ในอนาคต (เช่น `12/34`)
   - CVC: อะไรก็ได้ (เช่น `123`)
4. จ่ายเงินสำเร็จ → กลับมา Dashboard → credits เพิ่ม

### 🎵 ทดสอบ Transcription
1. ไปหน้า **Transcribe**
2. อัพโหลดไฟล์เสียง
3. เลือกภาษา → กด **"เริ่ม Transcribe"**
4. รอจน status เป็น ✅ → ดาวน์โหลด JSON

---

## ไฟล์ .env.local ตัวอย่างเต็ม

```env
# Auth
AUTH_SECRET=xxxxx  # auto-generated by npx auth secret

# Google OAuth
GOOGLE_CLIENT_ID=xxxxx.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-xxxxx

# GitHub OAuth (optional)
# GITHUB_CLIENT_ID=
# GITHUB_CLIENT_SECRET=

# Stripe (test mode)
STRIPE_SECRET_KEY=sk_test_xxxxx
STRIPE_PUBLISHABLE_KEY=pk_test_xxxxx
# STRIPE_WEBHOOK_SECRET=whsec_xxxxx  # ตั้งตอน deploy จริง

# Database
DATABASE_URL="file:./dev.db"

# Auth URL
AUTH_URL=http://localhost:3000
```
